<%= simple_form_for @purchase_reward do |f| %>
	<% if @purchase_reward.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(@purchase_reward.errors.count, "error") %> prohibited this reward from being saved:</h2>

		<ul>
			<% @purchase_reward.errors.full_messages.each do |msg| %>
			<li>
				<%= msg %>
			</li>
			<% end %>
		</ul>
	</div>
	<% end %>
	<%= f.input :title %>
	<%= f.input :points %>
	<%= f.button :submit %>
<% end %>

defaults: &defaults
  adapter: sqlite

mysql_defaults: &mysql_defaults
  adapter: mysql2
  username: deploy
  password: ZX5hEpyD0i
  host: localhost
  
development:
  database: db/genesis_development.db
  <<: *defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/genesis_test.db
  <<: *defaults
production:
  database: genesis
  <<: *mysql_defaults

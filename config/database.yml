sqlite_defaults: &defaults
  adapter: sqlite

mysql_defaults: &mysql_defaults
  adapter: mysql
  username: mysql_user
  password: mysql_sekret
  host: localhost
  
development:
  database: db/genesis_development.db
  <<: *sqlite_defaults

  # Add more repositories
  # repositories:
  #   repo1:
  #     adapter:  postgres
  #     database: sample_development
  #     username: the_user
  #     password: secrets
  #     host:     localhost
  #   repo2:
  #     ...

test:
  database: db/genesis_test.db
  <<: *sqlite_defaults
production:
  database: db/genesis_production.db
  <<: *mysql_defaults
